4月22日
    - 系统有点繁重，可以取消单专家的模式，只保留 router
    - 修prompt和逻辑冲突点、有很多prompt的语意重叠，这里说一遍，那里又一遍
    - 加一个没有 intake 的逻辑，方便跑测评
    - inference 不加 json 解析 retry 或者 封装成函数
    - 专家输出还是要分成三类的。。。

    ！！！ 专家不知道为啥没有结构化输出


    but 
        目前没有一个特别好的深入方向